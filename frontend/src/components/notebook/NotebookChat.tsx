import ChatWindow from "@/components/chat/ChatWindow";

export default function NotebookChat() {
  return (
    <main className="col-span-12 flex min-h-0 flex-col md:col-span-9">
      <ChatWindow />
    </main>
  );
}